app:
  telegram-token: ${TG_TOKEN}
  scrapper-api-base-url: http://localhost:8080
  retry-policy:
    mode: constant
    codes:
      - 500
      - 502
      - 504
spring:
  application:
    name: bot
  jackson:
    time-zone: UTC
server:
  port: 8090
logging:
  config: classpath:log4j2-plain.xml
#  level:
#    root: debug
#    org:
#      springframework:
#        web: trace
#      apache: trace
#debug: true

springdoc:
  swagger-ui:
    path: /swagger-ui

#rate-limit
bucket4j:
  enabled: false
